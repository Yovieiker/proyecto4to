const times = {
    // 1 day in milliseconds
    day: 1000 * 60 * 60 * 24,
    // 1 hour in milliseconds
    hour: 1000 * 60 * 60,
    // 1 minute in milliseconds
    minute: 1000 * 60,
    // 1 second in milliseconds
    second: 1000,
};

export { times };